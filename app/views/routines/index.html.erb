<h1>All Routines</h1>

# Users can see all routines created. but can not edit unless its their own
# add name of user that routine belongs to

  <% @routines.each do |routine| %>
      <h4><a href="/routines/<%= routine.id %>"></a></h4>
      <p><%= routine.title %></p>
      <br><br>
  <% end %>