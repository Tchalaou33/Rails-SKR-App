<h1>All Routines</h1>

# Users can see all routines created. but can not edit unless its their own

 <% @routines.each do |routine| %>
     <h4><a href="/routines/<%= routine.id %>"><%= 
     routine.created_at %></a></h4>
     <p><%= routine.title %></p>
     <p>By <%= list.user.name %></p>
     <br><br>
 <% end %>